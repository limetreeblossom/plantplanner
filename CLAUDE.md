# Project Instructions

## Before committing to Git

Always open `index.html` in the browser and verify the implementation works as expected before making a Git commit. Ask the user to confirm that the feature works correctly, then commit only after they give the go-ahead.
